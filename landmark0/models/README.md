# Landmark model implementations

Only the standalone full-frame baselines live here: the full multi-resolution
HRNet-W32 and ViTPose-S transformer/deconvolution model. YOLO26, V1 and V9 are
provided by the pinned source under `landmark0/_vendor/ultralytics` and selected
through YAMLs in `landmark0/cfg/models`.
